= link_to group_messages_path(group), class: "chat-list__link" do
  %li.chat-list
    %p.chat-list__name
      = group.name
    %p.chat-list__message
      = group.messages.present? ? group.messages.last.text : 'まだメッセージはありません。'
